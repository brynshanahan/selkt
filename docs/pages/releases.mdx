# Releases

## 2.0.0

Made `use-sync-external-store` a peer dependency of `@selkt/react`.
This is a breaking change as you will need to add `use-sync-external-store` to your project if you are using `@selkt/react`.
